# Database
DATABASE_URL="postgresql://lexbuild:password@localhost:5432/lexbuild"

# NextAuth
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Encryption (64 hex chars = 32 bytes) — generate with: openssl rand -hex 32
ENCRYPTION_KEY=""

# Storage: PostgreSQL bytea (templates, uploads, output .docx are stored inline in PG)
# No external S3/MinIO required.
